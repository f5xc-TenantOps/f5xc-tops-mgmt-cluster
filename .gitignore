# Bootstrap secrets (per-project)
bootstrap/tfc-bootstrap.yml
bootstrap/terrakube-bootstrap.yml
bootstrap/observability-bootstrap.yml
bootstrap/argocd-bootstrap.yml
bootstrap/terrakube-deploy-key

# Cluster-specific configuration
bootstrap/argocd-config.yml
bootstrap/argocd-ingress.yml
bootstrap/cert-manager-config.yml
bootstrap/coredns-config.yml

# Claude Code
.claude/

# Terraform state (managed by Terrakube, not stored locally)
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.backup